/// **转出，其他页面迁完即删**：页面头已移进组件库（`src/ui/PageHead.tsx`，从 `../ui` 取）。
/// 这里只留旧路径，免得还没迁的页面编译断
export { PageHead, PageTitle, PageHeadActions } from "../ui/PageHead.tsx";
export type { PageHeadProps } from "../ui/PageHead.tsx";
