name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  core-tests:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: swift --version
      - run: swift test
        working-directory: SymSyncCore
